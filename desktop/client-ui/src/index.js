/**
 * Host plugin for the Desktop client contribution package.
 *
 * The package contributes only a browser-side bundle, but Harness still loads
 * every client package through a host plugin entry.
 */
function apply() {}

export { apply }
